from gui.ventana import iniciar_app  
# Importa la función "iniciar_app" desde el archivo "ventana.py"
# que está dentro de la carpeta "gui"
que te cache pe

if __name__ == "__main__":  
# Esta condición verifica si este archivo se está ejecutando directamente
# (por ejemplo: python main.py)
# Si este archivo es importado desde otro, esto NO se ejecuta

    iniciar_app()  
    # Llama a la función "iniciar_app"
    # Esto normalmente inicia la interfaz gráfica (abre la ventana)
